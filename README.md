# HH Bot Dashboard

Веб-интерфейс для автоматизации работы с hh.ru: массовые отклики, авто-подъём резюме, AI-ответы работодателям, заполнение опросников нейронкой, детекция роботов-рекрутеров.

## Возможности

- **Массовые отклики** — параллельный сбор вакансий по нескольким поисковым URL, фильтрация, автоматическая отправка откликов
- **LLM авто-ответы** — DeepSeek/OpenAI отвечает на сообщения HR, учитывает резюме и историю переписки
- **Заполнение опросников** — нейронка заполняет анкеты работодателей при откликах
- **Детекция роботов-рекрутеров** — распознаёт чат-ботов HR, отвечает кнопками вместо текста
- **OAuth API** — отклики через официальный API hh.ru (опционально)
- **Умные фильтры** — по зарплате, формату работы, исключение агентств
- **Авто-подъём резюме** — поднимает резюме в поиске каждые 4 часа
- **Дневной лимит** — контроль количества откликов в день с автосбросом
- **Мониторинг** — просмотры резюме, приглашения, статистика переговоров
- **Мультиаккаунт** — несколько аккаунтов параллельно (браузерные сессии)
- **WebSocket** — обновление дашборда в реальном времени (300мс)

## Скриншоты

### Главная — карточки аккаунтов

![Главная](images/img.png)

На каждой карточке:
- Статус работы (сбор / отклики / лимит HH / дневной лимит)
- Счётчики: отклики, тесты, ошибки, интервью
- LLM статус: что делает нейронка прямо сейчас
- Прогресс-бар текущего цикла
- Тумблер авто-подъёма резюме
- Кнопки: пауза, LLM вкл/выкл, запуск LLM сейчас

---

### Лог — журнал событий

![Лог](images/img_1.png)

Живой лог всех действий бота:
- Отклики, опросники, ошибки
- LLM ответы с деталями
- Обновление в реальном времени по WebSocket

---

### Отклики — история

![Отклики](images/img_2.png)

Таблица всех откликов с поиском и сортировкой. Экспорт в CSV.

---

### Тесты — вакансии с опросниками

![Тесты](images/img_3.png)

Вакансии где требуется пройти тест/опросник.

---

### База — все вакансии

![База](images/img_4.png)

Локальная база собранных вакансий с фильтрацией и экспортом.

---

### HH Статус — переговоры

![HH Статус](images/img_5.png)

Переговоры с работодателями: интервью, просмотренные, отказы.

---

### Просмотры — аналитика резюме

![Просмотры](images/img_6.png)

Просмотры за 7 дней, приглашения, показы в поиске, история по компаниям.

---

### Ручной отклик

![Ручной отклик](images/img_7.png)

Ручная подача отклика: вставить ссылку, выбрать аккаунт, отправить.

---

### LLM Авто-ответы

![LLM Авто-ответы](images/img_8.png)

- Статус-бар: состояние LLM, интервал проверки, счётчики
- История ответов: сообщение HR → ответ бота → статус отправки
- Детекция роботов-рекрутеров (кнопочные ответы)
- Настройки: модель, промпт, профили, использование резюме

---

### Настройки

![Настройки](images/img_9.png)

- Параметры бота: задержки, лимиты, количество страниц
- Пул поисковых URL с индивидуальным числом страниц
- Фильтры: зарплата, формат работы, исключение агентств
- Шаблоны писем и опросников
- LLM настройки: ключ, модель, промпт, профили
- Управление куками и браузерными сессиями

---

## Быстрый старт

### Локально (разработка)

```bash
git clone <repo>
cd hh.ru-clicker
pip install -r requirements.txt
python web_app.py
```

Открыть: http://localhost:8000

---

## Деплой на VPS (продакшн)

### 1. Настроить переменные окружения

```bash
cp .env.example .env
# Сгенерировать надёжный пароль:
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Вставить результат в .env как DASHBOARD_PASSWORD=...
nano .env
```

### 2. Получить SSL-сертификат

**Вариант A — Let's Encrypt (рекомендуется):**
```bash
apt install certbot
certbot certonly --standalone -d ваш.домен.ru
cp /etc/letsencrypt/live/ваш.домен.ru/fullchain.pem ./nginx/certs/
cp /etc/letsencrypt/live/ваш.домен.ru/privkey.pem   ./nginx/certs/
```

**Вариант B — самоподписанный (только для теста):**
```bash
mkdir -p nginx/certs
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout nginx/certs/privkey.pem \
  -out nginx/certs/fullchain.pem \
  -subj "/CN=localhost"
```

### 3. Запустить

```bash
docker-compose up -d
```

Открыть: `https://ваш.домен.ru` — браузер запросит логин/пароль (Basic Auth).

### 4. Настроить автообновление сертификата (certbot)

```bash
# Добавить в crontab:
0 3 * * * certbot renew --quiet && \
  cp /etc/letsencrypt/live/ваш.домен.ru/fullchain.pem /путь/к/проекту/nginx/certs/ && \
  cp /etc/letsencrypt/live/ваш.домен.ru/privkey.pem   /путь/к/проекту/nginx/certs/ && \
  docker-compose restart nginx
```

### Архитектура деплоя

```
Интернет → nginx (443/TLS) → hh-bot:8000 (127.0.0.1 only)
```

- **nginx** — единственная точка входа, TLS-терминация, security-заголовки
- **hh-bot** — слушает только `127.0.0.1:8000`, недоступен из интернета напрямую
- **Basic Auth** — защита всех HTTP-запросов
- **WS-токен** — WebSocket аутентифицируется через `sha256(password)` в query-параметре
- **data/** — монтируется как volume, исходный код не монтируется в prod

---

## Структура проекта

```
hh.ru-clicker/
  web_app.py              # FastAPI app, BotManager, routes
  app/
    config.py             # Config, load/save настроек
    storage.py            # Кэш: applied, tests, interviews
    state.py              # AccountState — состояние аккаунта
    hh_api.py             # Парсинг страниц HH
    hh_apply.py           # Отправка откликов, опросники
    hh_chat.py            # Chatik API — переписка
    hh_resume.py          # Парсинг и аудит резюме
    hh_negotiations.py    # Статистика переговоров
    llm.py                # LLM интеграция (DeepSeek/OpenAI)
    questionnaire.py      # Парсинг и заполнение опросников
    oauth.py              # OAuth2 токены HH
    websocket.py          # WebSocket менеджер
    logging_utils.py      # Логирование
  static/
    index.html            # HTML структура дашборда
    css/style.css         # Стили
    js/app.js             # JS логика
  data/                   # Данные (не в git)
    config.json           # Настройки бота
    browser_sessions.json # Аккаунты и cookies
    applied_vacancies.json# История откликов
    test_vacancies.json   # Вакансии с тестами
    interviews.json       # Переговоры
```

## Данные

Все данные хранятся в `data/` (монтируется как Docker volume):

| Файл | Описание |
|------|----------|
| `config.json` | Настройки: URL пул, лимиты, LLM конфиг |
| `browser_sessions.json` | Аккаунты: имя, cookies, resume_hash |
| `applied_vacancies.json` | История откликов (по аккаунтам) |
| `test_vacancies.json` | Вакансии с опросниками |
| `interviews.json` | Переговоры и чаты |

---

## Технологии

| Компонент | Стек |
|-----------|------|
| Backend | Python 3.11, FastAPI, uvicorn |
| Frontend | Vanilla JS, WebSocket (real-time 300ms) |
| AI | DeepSeek / OpenAI совместимый API |
| Парсинг | requests, aiohttp, SSR JSON, regex |
| Чат | chatik.hh.ru API (reverse-engineered) |
| OAuth | HH Android app credentials |
| Деплой | Docker + docker-compose |

---

## Требования

- Python 3.10+
- Cookies активной сессии hh.ru (hhtoken, hhuid, _xsrf, crypted_id)
- API ключ DeepSeek/OpenAI (для LLM-функций, опционально)
